{
  "name": "porter",
  "version": "0.0.1",
  "description": "💁 the build system to carry your projects",
  "scripts": {
    "build": "yarn workspaces foreach -tvp run build"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
